validateFirstRun(document.currentScript);

/**
 * This function parses and executes the script passed in
 * @param script
 *        {string}
 */

function executeScript(script) {
    //todo: design and code functionality
}